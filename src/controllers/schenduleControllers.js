// coletar os valores da data de abertura e data de fechamento

// coletar os valores das datas de restrições
// guardar essas restrições em uma lista

const registerNewSchendule = (req, res1)=>{
}

module.exports = {
    registerNewSchendule
}